URL Shortener

Design a URL Shortener service that takes in a valid URL and returns a shortened version of that, which redirects to the same page.

Also keep track of total visit/clicks on the URL

Routes

POST /URL - Generates a new short URL and returns it 

GET /:id - redirects the user to the original URL

GET /URL/analytics/:id - returns the clicks for the provided short id

